# bd-radar — 2026-06-11

who's building, forking, integrating, mentioning aeon + miroshark → ranked leads + one move each.

**13 candidates surfaced · 10 in digest.** First real bd-radar sweep — dedup baseline (no prior `surfaced` set). Cross-checked against `ECOSYSTEM.md` + `SHOWCASE.md`; known operators tagged *(known — expanding)*.

## Ranked leads

| # | Class | Who | Signal | Fit | Move |
|---|-------|-----|--------|-----|------|
| 1 | `building` | **danbuildss/luca-aeon-skills** — [@AskLucaAI](https://x.com/AskLucaAI) (x402Books) | NEW builder. 4 financial-intel skills for Aeon (wallet scan, treasury, reports, agent registry) on Base. Shipped 06-04. | wedge (x402/data) | DM @AskLucaAI — fresh x402-native skill pack, invite to the aeon TG, offer to list in ECOSYSTEM |
| 2 | `building` | **UIZorrot/careful-finance-aeon-skill-pack** — Careful Finance | *(known — expanding)* fork operator went fork→skill-pack: market-intelligence pack, shipped 06-08 (freshest). | wedge (data) | reply UIZorrot — they doubled down, ask what's next, feature the pack |
| 3 | `building` | **codexvritra/signa-aeon-skills** — [@Signa_Agent](https://x.com/Signa_Agent) | *(known — expanding)* new repo 06-07: wallet-signed cross-platform agent messaging pack. | wedge (agent infra) | ping Signa — x402/messaging overlap, scope a deeper integration |
| 4 | `building` | **Zoidberg-eternal/monitor-the-situation-bags** — ZERA / Zero Human Labs | NEW. **Vendors MiroShark** as the sim engine for a Bags Hackathon 2026 token-launch forecaster (Neo4j graph). | wedge (sim) | DM ZERA — real MiroShark-in-the-wild integration, invite + offer the x402 sim hook |
| 5 | `building` | **basedcryptoji/solvr-aeon-skill** — Solvr | *(repeat builder)* 2nd aeon pack after Clerk: formal verification via x402 ($0.001 USDC/Base). | wedge (x402) | reply basedcryptoji — two products on aeon now, partner candidate |
| 6 | `building` | **JohnTammi/supercompact-for-miroshark** ⭐4 | NEW. A context-compaction tool built *for* MiroShark. | wedge (sim tooling) | DM JohnTammi — tooling builder, link the miroshark TG, ask what broke that made them build it |
| 7 | `forking` | **XIAOCUICUII/koc-growth-agent** | NEW. Rebuilt a KOC growth-prediction system *based on* MiroShark (CN). | wedge (sim/growth) | translate the README + reach out — CN growth-prediction use case worth understanding |
| 8 | `building` | **Homie4570/aeon-skills** — LoneStarOracle | NEW. weather/crypto/macro skill pack on aeon. | general | low-touch — note it, watch if it moves on-wedge |
| 9 | `building` | **baseddevoloper/aeon-skill-pack-vvvkernel** | NEW. vvvkernel skill pack (no description yet). | general | take a look, classify next sweep |
| 10 | `adjacent` | **wshuyi/crucible-sim** ⭐7 | Adversarial swarm-sim on MiroFish+OASIS. Name-checks MiroShark but "miroshark mode" is just a persona preset — **doesn't call MiroShark**, rolled their own. OpenRouter-compatible. | wedge (sim) | outbound — sim-wedge builder *not* on us. DM wshuyi: pitch the MiroShark engine vs maintaining their own harness |

## Read

The fresh inbound is **financial-intelligence skill packs** (luca, Careful Finance, Solvr) — all x402/Base, all this week. That's a cluster, not noise: aeon is becoming the scheduled-execution layer behind on-chain data products. Lead #1 (luca / x402Books) is the only brand-new name in it — reach out while it's warm.

MiroShark's in-the-wild signal is thinner but real: one genuine vendored integration (monitor-the-situation-bags, Bags hackathon) and one tooling builder (supercompact). crucible-sim is the tell — a sim-wedge team that name-checked MiroShark but built on MiroFish+OASIS instead. That's an outbound, and a hint: lower the cost of building *on* the engine before more people roll their own.

## Source status / misses

- `BD_RADAR_SOURCE_MISS: github-forks (403 — runner GITHUB_TOKEN is integration-scoped to aeon-nur; forks API on aaronjmars/aeon + MiroShark not accessible)`
- `BD_RADAR_SOURCE_MISS: github-issues (403 — same token scope; can't read aeon/MiroShark issues for the integrating signal)`
- `BD_RADAR_SOURCE_MISS: github-code-search (429 — rate limited)`
- `BD_RADAR_SOURCE_MISS: x (no .xai-cache/bd-radar feed on the runner; x-mcp is local-only)`
- **OK:** `gh search repos` (the building/forking signal), WebSearch + WebFetch (verification), `SHOWCASE.md` (fork dedup snapshot, regenerated Sundays).
- **Fix the eyes:** the two strongest classes — live `forking` and `integrating` — are dark because the runner token can't hit the forks/issues API on the product repos. Follow-up: add `aaronjmars/aeon` + `aaronjmars/MiroShark` to the `GH_READ_PAT` resource list (or use a classic read token in a prefetch) so bd-radar gets live forks + issues, not just `gh search`.

---

## Update — `forking` source now lit (post #4)

The morning sweep flagged forks/issues as dark (runner token 403). Commit **#4** wired the read-only `GH_READ_PAT` prefetch (`scripts/prefetch-private-repos.sh` → `.xai-cache/bd-radar-github.json`), so the live `forking` signal is now visible for the first time: **40 aeon forks + 40 MiroShark forks**. "fix the eyes" landed.

Kept forks with their own commits (`pushed` after `created`); dropped drive-by syncs and deduped owners already in `ECOSYSTEM.md` / `SHOWCASE.md` (Aeon City, AntFleet, Atrium, Echo Oracle, Hound Flow, Mneme, VIGIL) and the operator's own fork (NurstarK). **12 fresh forking leads** survived. Top 10:

| # | Class | Who | Signal | Fit | Suggested move |
|---|-------|-----|--------|-----|----------------|
| 1 | `forking` | **swarm-ai-research/aeon-atlas** — likely [@ResearchSwarmAI](https://x.com/ResearchSwarmAI) | **Cross-product.** Forked aeon → renamed `aeon-atlas` (~52MB, +22MB of own content over base) **and** MiroShark — both pushed **today**. Known ecosystem name, but this is a hard escalation onto both products. | wedge (agent infra + sim) | DM ResearchSwarm — going deep on aeon *and* miroshark at once. Ask what aeon-atlas is, get them in both TGs, flagship-fork candidate |
| 2 | `forking` | **anondevv69/bankr-space-aeon** | Named new aeon fork `bankr-space-aeon` (33MB, own content), created 06-10, pushed **today**. Bankr-adjacent (Bankr in ECOSYSTEM). | wedge (agent infra) | check what "bankr-space" is — if it's a real Bankr surface on aeon, route through the Bankr relationship |
| 3 | `forking` | **ScoutAeon/Scout** | Renamed aeon fork → `Scout` — an intentional named product, not a default fork. | wedge (agent infra) | DM ScoutAeon — renamed = they're shipping something, ask what Scout does |
| 4 | `forking` | **dan-and/MiroShark** | Active MiroShark fork w/ own commits (62.8MB), 06-03 → 06-08. | wedge (sim) | watch + light DM — real miroshark builder, link the sim TG |
| 5 | `forking` | **CertifiedCryp/MiroShark** | MiroShark fork w/ added content (62.8MB), 05-23 → 06-06. | wedge (sim) | low-touch ping, ask what they're simulating |
| 6 | `forking` | **DYAI2025/MiroShark** | MiroShark fork, recent activity 05-22 → 06-10. | wedge (sim) | watch — note if it ships |
| 7 | `forking` | **daxaur/aeon** | Active aeon fork, own commits 06-05 → 06-10. | wedge (agent infra) | watch — new operator running their own instance |
| 8 | `forking` | **beijiangqukuailian/aeon** | aeon fork, 8d active 06-03 → 06-11 (pushed today, CN). | wedge (agent infra) | watch — CN operator, persistent activity |
| 9 | `forking` | **TakamiyaZee/aeon** | aeon fork pushed **today**, 05-28 → 06-11. | wedge (agent infra) | watch — still active two weeks in |
| 10 | `forking` | **Raul-dd/MiroShark** | MiroShark fork w/ own commits 05-27 → 06-02. | wedge (sim) | watch |

*(+2 below the cut: `chxoky/aeon`, `Damoncrypto/aeon` — both real own-activity aeon forks, score 12.)*

## Read (forking)

The fork fleet is healthy and **two-sided**: aeon and miroshark are each pulling ~40 forks, and the same operators are starting to take *both* — `swarm-ai-research` (aeon-atlas + MiroShark, today), plus `seuzht`, `BuiltByEcho`, `SahilParikh03`, `LiamVisionary` all hold a fork of each. That cross-pollination is the strongest health signal here: the wedge isn't two separate audiences, it's one builder pool adopting the whole stack.

The single lead worth a message today is **swarm-ai-research / ResearchSwarm** — renaming aeon to `aeon-atlas`, adding 22MB of their own content, and forking MiroShark the same day is not a drive-by. Everything else is "watch," not "reach out."

`integrating` is now genuinely **empty, not dark** — the issues endpoint returned `[]` for both aeon and MiroShark (0 open integration-ask issues), so that class is a true zero this sweep, not a source miss.

## Source status (update)

- **NOW OK:** `github-forks` — `.xai-cache/bd-radar-github.json` via `GH_READ_PAT` prefetch (40 aeon + 40 MiroShark forks). The morning 403 is resolved.
- **OK (empty):** `github-issues` — endpoint returned `[]` for both repos. 0 open integration-ask issues; `integrating` is a true zero, not a miss.
- **Still dark:** `x` (no `.xai-cache/bd-radar` X feed on the runner; x-mcp local-only) — the `mentioning` class stays unlit until the X prefetch is wired for bd-radar.
- **No notify:** all 12 fresh leads are `forking` (not `building`/`integrating`), so this update stays quiet per the skill's notify gate — the daily `war-room` brief carries the roll-up.
