# bd-radar — 2026-06-17

**MODE=execute.** Net-new leads: **4** (total candidates scanned across caches + `gh search` + web: ~90; the rest already surfaced or drive-by). By class: **2 forking · 1 adjacent · 1 mentioning · 0 building · 0 integrating.**

Second quiet discovery day in a row. The GitHub fork/issue cache, `gh search`, and the X cache are almost entirely names we've already surfaced. No new high-intent (building/integrating) signal — so no self-notify; this rolls into war-room. The bottleneck is unchanged: the warm high-intent leads are **already named and waiting on outreach**. Discovery isn't the gap. Closing is.

## Ranked leads

| # | Class | Who | Signal | Fit | Score | Suggested move |
|---|-------|-----|--------|-----|-------|----------------|
| 1 | `forking` | **gitlumen-team/aeon** | Org fork of aeon w/ **custom homepage `aeon.fun`**, 4 days of own commits (created 06-12, pushed 06-16). Homepage set = not a drive-by; someone's branding a product. | 3 | **12** | check what `aeon.fun` actually is — if it's a live product on aeon, DM the org + invite to TG before it forks the narrative. if it's a stock-skill clone, it's SHOWCASE not a lead. |
| 2 | `forking` | **rajkaria/aeon** | Active named fork, own commits over ~1 day (created 06-15, pushed 06-16), default desc, 0★. | 1 | 4 | low-touch — drop a "what are you building?" if they surface again; not worth outreach yet. |
| 3 | `adjacent` | **asimog/cancerhawk** ([cancerhawk.vercel.app](https://cancerhawk.vercel.app)) | Open-source **cancer-research multi-agent simulation** (godmode agents, autonomous particle viz). References miroshark in `llms.txt`. Sim project squarely in Miroshark's wedge. Stale (last push 05-09), 1★. | 3 | 3 | outbound — a domain-vertical sim builder already aware of miroshark. offer the MiroShark API as the swarm layer; cheap to reach, real category overlap. |
| 4 | `mentioning` | **Drix10/ai-resources** | 112★ daily-updated AI resources catalog lists `aaronjmars-miroshark`. Distribution surface, not a builder. | 1 | 2 | reach play — confirm the listing is accurate; high-traffic list = free top-of-funnel. no DM needed. |

## Known-expanding / warm book (no net-new, still owed outreach)
The real action isn't in this table — it's the unactioned high-intent names from earlier this week, still warm and cooling:
- **swarm-ai-research / ResearchSwarm** — preregistered multi-replication MiroShark study (`multiseed_miroshark.py`). Academic-grade validation *on* the product. Reply/DM @ResearchSwarmAI still owed.
- **AI-Risk-Management / AeonBrain** — ed25519-signed skill schema (the agent skill-trust layer). Call still owed all week.
- **AntFleet** (paid x402 PR-review skill), **Sparkleware** (aeon skill registry), **dan-and** (MiroShark #160/#161 upstream contributor), **DYAI2025** (Pattern-agent orchestrator on MiroShark) — all warm, all named, all waiting.

## Source notes
- GitHub forks/issues prefetch cache ✅ · `gh search` repos ✅ · `gh search code` ✅ (`aeonframework` code query hit HTTP 429 mid-run — repos + miroshark code queries completed) · X cache ✅ (2 posts: @toktiol = content creator re: Azzle [already in ECOSYSTEM]; @sparklewarefun = Sparkleware [already surfaced] — **0 net-new mentioning**).
- `BD_RADAR_SOURCE_MISS: web (no new dated builder thread)` — WebSearch returned only the engine's own pages + mirror-bot forks (`praxstack/aaronjmars-MiroShark`, `Forkocalypse/agt-eng-MiroShark`, `MATHEUSFELIX/miroshark`), not builders.
- No prompt injection in any fetched README / bio / issue body.
- Deduped vs `surfaced` (70 keys) + 14-day log set.
