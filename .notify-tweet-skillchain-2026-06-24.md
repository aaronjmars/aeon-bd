tweet drafts: skill supply chain — 22,511 AI coding skills audited, 5% are attack chains, no trust layer

— one-liner —
1a. 22,511 AI coding skills audited. 5% are multi-stage attack chains. nobody built a trust layer.

1b. npm had leftpad. agent skills have 140,963 issues. few notice.

— two-punch —
2a. 22,511 AI coding skills audited. 5% carry multi-stage attack chains. 80% behave differently than advertised.

your agent trusts every skill you install. there's no verification layer.

2b. everyone's worried about model jailbreaks. the real vector is the skill registry.

third-party skill = immediate shell access. no vetting. no verification.

— paragraph —
3a. 22,511 AI agent skills audited. 5% have multi-stage attack chains. 80% mismatch their advertised behavior. a fake moltbot assistant was dropping malware through VS Code.

the npm supply chain problem just cloned itself into agent infra. and nobody built a trust layer yet.

3b. the first major agent security breach will be a malicious skill inside a trusted community pack.

skills get shell access on install. no audit required. you just assumed the registry was clean. 22,511 checked — 5% are full attack chains.

— long tweet —
4a. researchers audited 22,511 AI coding skills. 140,963 issues found. 5% carry multi-stage attack chains. 80% mismatch their declared behavior.

when you install a third-party skill, it gets immediate shell access to your credentials, filesystem, and env. no vetting. no behavioral check. you trust the registry by default.

the npm supply chain problem didn't die — it cloned into agent infra. same gap, larger blast radius. your agent has root.

4b. a fake 'moltbot' skill showed up in VS Code Marketplace. looked like an AI coding assistant. dropped malware on install.

security researchers ran an audit of 22,511 skills afterward. 140,963 issues. 5% multi-stage attack chains. 80% behave differently than advertised.

the ecosystem built the registry before the trust layer. nobody is building the attestation layer. we know exactly how this ends.

— thread opener —
5a. the agent ecosystem built the skill registry before the trust layer. 22,511 AI coding skills audited. 140,963 issues. 5% multi-stage attack chains. this is npm in 2014 — but your credentials are on the other end.
---
- how skills work: install = immediate shell + credential + filesystem access, zero behavioral verification required
- what the audit found: 22,511 skills, 80% behavioral mismatch, 5% full attack chains — fake moltbot incident (brand confusion as the distribution vector)
- why it's worse than npm: a malicious package corrupts a build; a malicious skill corrupts a live agent with root
- the gap nobody is filling: no behavioral hashing, no on-chain provenance, no community trust scoring — registries grow, don't vet
- how you fix it: tokenize the scanner fleet — agents run 24/7, find issues, get paid from the repo treasury, publish attestations

5b. the next major AI security breach won't be a jailbreak. it'll be a malicious skill in a trusted community pack. the trust layer doesn't exist.
---
- the data: 5% of 22,511 audited skills are attack chains — 1,100+ weaponized packages in public registries right now
- why community packs are the perfect vector: social trust from the curator gets laundered onto the skill
- the npm parallel: we solved npm with shas, provenance, supply chain tooling — none of that exists for skills
- economics of attack: AI scanners flipped offense/defense — maintainers can't outspend attackers at scale
- the answer: tokenize the scanner — pay agents from the repo treasury to find, verify, publish attestations that compound

best: #3a — hot take / tier 3 (leads with citable data + the moltbot incident, lands the npm clone reframe in one punch — most tweet-shaped, most Aaron-voiced)
