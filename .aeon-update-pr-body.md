## Upstream sync: `aeonfun/aeon` `c28ab4c..95142d1`

**16 commits** (2026-09-10 → 2026-09-12) · **21 applied** · **13 manual** · baseline → `95142d1`.

Framework delta since PR #99. The bulk is the upstream **vuln-scanner "Riva" research kernel** (new scripts + tests), read-only-guard fixes, and dependabot bumps. All new helper scripts and their CI test files landed clean; the workflow *wiring* that references them stays a conflict because this fork narrowed those workflows.

### Applied cleanly (21)
- **New scripts (5):** `scripts/build-vuln-context.sh`, `scripts/compare-riva-shadow.sh`, `scripts/git-push-retry.sh`, `scripts/resolve-riva-capabilities.sh`, `scripts/validate-riva-candidates.sh`
- **New CI tests (8):** `scripts/tests/test_chain_runner_no_action.sh`, `test_readonly_failure_commit.sh`, `test_readonly_guard_log.sh`, `test_riva_candidates.sh`, `test_riva_context.sh`, `test_riva_kernel_contract.sh`, `test_riva_shadow_compare.sh`, `test_riva_shadow_guard.sh`
- **New skill doc:** `skills/vuln-scanner/riva.md` _(a doc under an existing, disabled skill — no new lock/glyph needed)_
- **Updated (4):** `.github/workflows/chain-runner.yml`, `apps/webhook/package.json`, `docs/ECOSYSTEM.md`, `scripts/tests/test_vuln_scanner_status.py`
- **Auto-merged (3-way):** `.github/workflows/aeon.yml`, `apps/webhook/package-lock.json`, `skills/github-trending/SKILL.md`  _(your local narrowing kept; upstream's disjoint changes applied — review the merged hunks; all re-validated: YAML/JSON parse + `validate-config.js` PASS)_

_No catalog regeneration: the applied `skills/**` changes touch no frontmatter/glyph/pack field, so `skills.json`/`packs.json`/`skill-icons` are unchanged. (Note: `bin/generate-skill-icons --check` flags a **pre-existing** drift on `competitor-monitor.svg`, a fork-local skill unrelated to this delta — left untouched to keep the sync scoped.)_

### New upstream skill deferred (adopt manually if wanted)
- **`miroshark-matchday`** (#1056, `f891ede`) — weekly bulk football matchday **MiroShark** sims + video handoff (Fri 09:00, `enabled:false`, pays $1 USDC/run via x402). **Product-relevant to this war room.** Not auto-added: `catalog/skill-icons.json` (operator-owned) has no `miroshark-matchday` glyph, so `bin/generate-skill-icons` would fail CI. To adopt: copy `skills/miroshark-matchday/` from upstream, add a glyph to `catalog/skill-icons.json`, run `eyebrow scan --path . --lockfile eyebrowlock.json`, add the `aeon.yml` registry line below, then `bin/generate-skills-json && bin/generate-packs-json && bin/generate-skill-icons`.

### Needs manual review — conflicts (your local copy diverges from upstream)
- **`.github/workflows/ci-tests.yml`** — upstream added an `.github/workflows/aeon.yml` trigger path plus new test steps (read-only guard-log/failure-commit, chain-runner no-action, 5× Riva). **All referenced test files were synced clean in this PR** — only the workflow wiring conflicts with your narrowed copy. Merge the new `- name:` steps you want:
  ```
  + read-only guard run-log tests        → scripts/tests/test_readonly_guard_log.sh
  + read-only failure-commit tests       → scripts/tests/test_readonly_failure_commit.sh
  + chain-runner no-action status tests  → scripts/tests/test_chain_runner_no_action.sh
  + vuln-scanner Riva {context,shadow-guard,shadow-compare,candidates,kernel-contract} tests
  ```
- **`.github/README.md`** — upstream only bumped the catalog count `80 → 81` + full-catalog anchor (miroshark-matchday). Trivial; your narrowed README intentionally doesn't track the canon count.
- **`docs/skill-packs.md`** — upstream bumped the count `80 → 81` and added `miroshark-matchday` to the **Crypto & Markets** pack row. Overlaps your narrowed pack tables.
- **`skills/vuln-scanner/SKILL.md`** — your fork ships a simplified copy; upstream rewrote it substantially (Riva research kernel `19aafe6`, bounded-trufflehog fs scan + no-backgrounding guard `90c369f`, feed-parseability `d1ca7e7`). Skill is `enabled:false` here — reconcile only if you plan to re-enable it.

### Carried forward — unresolved from prior runs (not touched this window)
`.github/workflows/messages.yml`, `CHANGELOG.md`, `llms.txt`, `skills/heartbeat/SKILL.md`, `skills/memory-flush/SKILL.md`, `.github/dependabot.yml`, `skills/compute-resell/` + `skills/submit-hook/` (both deferred new skills, same glyph/lock reason as miroshark-matchday). See `memory/topics/aeon-update-state.json`.

### Operator config changed upstream (not auto-applied — reconcile by hand)
- **`aeon.yml`** — upstream added one registry line (surfaced, not written):
  ```yaml
  miroshark-matchday: { enabled: false, schedule: "0 9 * * 5", var: "" } # Friday — one MiroShark matchday sim per league
  ```
- **`catalog/skills.json` / `catalog/packs.json` / `eyebrowlock.json`** — upstream counts bumped for the 3 deferred skills; kept the fork's narrowed versions (these are regenerated from synced sources, never copied). No regen delta this run.

### Upstream commits
| SHA | Summary |
|-----|---------|
| 95142d1 | fix(ci): add .github/workflows/aeon.yml to ci-tests trigger paths (#1058) |
| 9bf02fb | docs: update Hunch logo image |
| 3fa3bef | fix(tests): make vuln-scanner status test tolerant of the timeout-aware block (#1057) |
| f891ede | Add miroshark-matchday skill: weekly bulk football matchday sims + video handoff (#1056) |
| d1ca7e7 | fix(vuln): keep github-trending feed machine-parseable for the scanner (#1055) |
| 90c369f | fix(vuln-scanner): bound the trufflehog filesystem scan, forbid backgrounding scanners (#1054) |
| 244f7ac | fix(chain): distinguish no-action telemetry (#1053) |
| 19aafe6 | feat(vuln-scanner): add Riva research kernel (#1039) |
| 3ffe60d | fix(aeon): commit a read-only skill's failure log, not just its success (#1052) |
| 5573848 | fix(aeon): read-only guard stub loses every skill's memory/logs baseline (#1051) |
| 5d005b6 | chore(deps): override sharp to 0.35.4 in apps/webhook (#1050) |
| a3d402c | chore(deps-dev): bump js-yaml in /apps/dashboard (#1049) |
| c554e8b | chore(deps): bump sharp in /apps/dashboard (#1048) |
| 4d9e42a | chore(deps): bump js-yaml (#1047) |
| 58b2e53 | chore(deps-dev): bump js-yaml in /apps/webhook (#1046) |
| 7e1c6ad | chore(deps): bump hono in /apps/mcp-server (#1044) |

---
🤖 aeon-update — 3-way sync. Applied files carry your local customizations where they didn't overlap upstream. Merge advances the baseline to `95142d1`.
