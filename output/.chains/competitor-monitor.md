⚠️ Competitor Monitor

*Competitor Monitor* — first run, 1 page on watch list (nvidia.com/en-us/security/acknowledgements). Fetched OK (200, 6.3k words) but couldn't persist the snapshot this run — memory/ is mounted read-only in this environment (EROFS on mkdir/writeFileSync), so there's no baseline saved for next run to diff against. Flagging as an infra issue, not a competitor signal.