# BD Radar — 2026-08-27 (run 2)

vs 2026-08-27 (earlier run today) — 2 new — 29 still-open — 0 dropped — 31 leads total

| # | Class | Who | Signal | Fit | First seen | Move (expiry) |
|---|-------|-----|--------|-----|-------------|----------------|
| 1 | `building` | [jsfranklin221/aeon-skill-pack-spoolis](https://github.com/jsfranklin221/aeon-skill-pack-spoolis) | [Spoolis](https://spoolis.com) (outcome-verification product) shipped an Aeon skill pack today, ~1hr after `git init` — signed Ed25519 Outcome Receipts that verify a chain step's deliverable against explicit acceptance criteria, keyless sandbox mode, passes Aeon's own `validate-pack.sh` | 3 | today | DM jsfranklin221 — thank them for building on Aeon, invite to `pack-submit` / `docs/ECOSYSTEM.md` — flips if: repo goes stale (no push 14d) |
| 2 | `building` | [amritmirch/aeon-skill-pack-farcaster](https://github.com/amritmirch/aeon-skill-pack-farcaster) | Individual builder shipped Aeon's first Farcaster skill pack today — `cast` via Neynar, fail-closed gauntlet, daily cap — fills a real gap (Aeon only ships X-native social skills today) | 3 | today | DM amritmirch re: the pack, but verify identity/intent first — same handle was flagged today by engagement-act for recycling a debunked fake-star-count claim on X — flips if: repo goes stale (no push 14d) |

**29 leads from prior runs today/this week remain live/unchanged** (CultOS, JessieJanie/aeon-skill-pack-skim, Finance District ×2, Verdikta, LSO-AI, AI2Human, AgentLink, Sparkleware ×8, czijian729-oss/aeon, raul1stdigital/aeon ×2, and 14 more) — already surfaced, excluded from this digest; see `memory/topics/bd-radar-leads.json` for the full live set.

**Also checked, no new signal:** GitHub forks of all 4 configured repos (drive-by forks only — `jsfranklin221/aeon` and `thesmithdao/aeon` both 0-1 commits ahead of upstream, not counted as leads separately from their skill-pack repos above); GitHub issues (both open issues already known — aeon#878, MiroShark#240); `gh search code` for `aeonfun/aeon` (only awesome-list catalogs + self-repo hits); X `x_search` (2026-08-24→08-27, HTTP 200, 2 posts matched — one is @aaronjmars himself, the other a 21-view zero-engagement retail post with no project/builder signal, both excluded); WebSearch sweep for "built on aeon" (name-collision noise only — ML framework, openSUSE Aeon, Monero fork).

**Dropped as noise:** none this pass (the X hit was excluded for lack of builder signal, not a formal drop).

run: github-forks-issues ok (4/4 repos, default token) · gh-search ok · x_search ok (http=200) · websearch ok · no BD_RADAR_SOURCE_MISS

## Summary
- Second bd-radar pass today, following an earlier same-day run that already surfaced CultOS + 13 other leads (logged separately in `memory/logs/2026-08-27.md`).
- Re-swept GitHub forks/issues (4 configured repos), `gh search repos`/`code` discovery, X `x_search`, and WebSearch — found 2 new `building`-class leads not caught by the earlier pass: a third-party product (Spoolis) integration and an individual builder's Farcaster skill pack, both created/pushed today.
- 29 leads from prior runs (today and this week) remain open/unchanged, excluded from this digest per dedup.
- Files updated: `output/articles/bd-radar-2026-08-27.md` (this file, overwritten to reflect the cumulative state for today), `memory/topics/bd-radar-leads.json` (2 new leads appended, 31 total; surfaced LRU now 31 keys), `memory/logs/2026-08-27.md` (new `### bd-radar` block appended for this pass).
- Notified: yes — both new leads are `building` class, meets the self-notify bar.
- Follow-up: Aaron to review the Spoolis integration (a real third-party product building an Aeon connector) and decide on the Farcaster pack outreach given the builder's mixed track record (real code, but a prior flagged fake-metrics post).
