## Upstream sync: `aeonfun/aeon` `3b4c5a3..bf33365`

**22 commits** (2026-09-01 → 2026-09-04) · **27 applied** · **12 manual** · baseline → `bf33365`.

Auto-generated by `aeon-update`. Clean framework changes are applied on this branch; operator-owned and conflicting files are surfaced below for you to reconcile by hand. Merging this PR advances the sync watermark to `bf33365`.

### ✅ Headline: last week's GLM-gateway conflict is resolved
`scripts/llm-gateway.sh` — the PR #77 conflict (our incident pin `04d56d5` vs upstream's own GLM routing) is **gone**. Now that PR #77 landed upstream's gateway, the only new upstream change here (`dc1a975`, pin GLM reasoning effort to `high`, var-overridable via `GLM_REASONING_EFFORT`) was **already present in your local copy**, so the 3-way merge is a content no-op (only a benign `644→755` mode bump). No more carrying it forward.

### Applied cleanly (27 files)
- **Auto-merged (3-way, your local edits kept + upstream's disjoint changes applied):**
  - `scripts/llm-gateway.sh` — see headline (content no-op).
  - `.github/workflows/ci-skill-integrity.yml` — eyebrow gate bump `v0.4.1 → v0.4.2` (#1002), disjoint from your edits.
  - `skills/feature/SKILL.md` — upstream's dev-loop dispatch-correlation-ID handoff (#1009), disjoint from your flat frontmatter.
  - `skills/skill-health/SKILL.md` — upstream's critical-incident recovery detection (#1018), disjoint from your scorer customization.
- **New scripts:** `scripts/dev-loop-pr.sh` (#1009), `scripts/skill_health_recovery.py` (#1018), `scripts/vuln-poc-gate.sh` (#1008), + 9 test files under `scripts/tests/` (fleet-scorecard, llm-gateway effort, skill-health recovery, chain-runner invalid-dispatch, dev-loop handoff, vuln-poc gate, + foundry PoC fixtures).
- **Scripts / harness:** `scripts/fleet-scorecard.mjs` (#1019 — stop non-skill runs leaking into per-skill table), `scripts/notify-deliver.sh` (#1020 — per-channel delivery status), `scripts/skill_mode.sh`, `scripts/stage-vuln-scanner.sh`.
- **Workflows:** `.github/workflows/chain-runner.yml` (#1017 — a rejected dispatch no longer poisons the reliability signal).
- **Docs:** `docs/CONFIGURATION.md`, `docs/ECOSYSTEM.md` (removed MythosForge, #1010), `docs/skill-integrity.md` (#1001 — optional MCP `eyebrow wrap` hardening), `plugin/skills/aeon/references/secrets.md`.

### Needs manual review (conflicts — your local copy diverges from upstream)

**Low urgency — 4 of the conflicting skills are `enabled: false` in your `aeon.yml`** (pr-review, vuln-scanner, skill-health*, feature*; *the last two auto-merged, listed above). Reconcile only what you care about.

- **`.github/dependabot.yml`** — near-trivial. Upstream added eslint/globals major-version holds (#1012); **you already applied the identical holds** — the only difference is your extra `(tracking as of 2026-09-01)` parenthetical. Nothing to do unless you want to drop the note.
- **`.github/workflows/aeon.yml`** — your env-block is narrowed; upstream added, in overlapping regions: `GLM_REASONING_EFFORT` env wiring (3 spots), the **block-style `model:` scheduler fix (#1024)**, a Foundry install step + PoC-verdict capture for vuln-scanner, a dispatch-correlation-ID prompt block, and `actions/upload-artifact v4→v7`. **Note on #1024:** it fixes reading `model:` from *block-style* `aeon.yml` entries — but your only two model overrides (`aeon-update`, `idea-forge`) are single-line, which the old code already read correctly. So this fix is good hygiene but **not currently affecting your runs**; adopt it if you ever move a model-pinned skill to block style.
- **`.github/workflows/ci-tests.yml`** — your CI is narrowed; upstream added 6 new test steps for the scripts above (fleet-scorecard, llm-gateway, skill-health recovery, chain-runner invalid-dispatch, dev-loop handoff, vuln-poc gate). Add the ones whose scripts you now carry if you want them gated in CI.
- **`.github/workflows/messages.yml`** — one line: upstream added `GLM_REASONING_EFFORT` to the reply-job env (#420dc85). Add it to your narrowed env block if you use GLM for conversational replies.
- **`CHANGELOG.md`** — append-region conflict (your changelog diverges); upstream added entries for eyebrow v0.4.2, GLM effort pin, MCP hardening docs. Cosmetic — merge the entries you want.
- **`skills/pr-review/SKILL.md`** *(disabled)* — your fork uses flat `type: Skill` frontmatter + `aaronjmars/aeon` repo naming; upstream added `owner/repo#N` single-PR scoping in the same regions. Adopt the `#N` scoping if you ever enable this skill.
- **`skills/vuln-scanner/SKILL.md`** *(disabled)* — your fork carries a simplified copy (flat frontmatter, older osv-scanner path); upstream added a whole **PoC-verification gate** (A4.5 — HIGH/CRITICAL findings must survive an executable verifier) + a `poc-smoke` arm (#1008). Deep divergence; take upstream wholesale if you ever run security audits here.

### Carried conflicts (unchanged this window — still awaiting your reconciliation)
`.github/README.md`, `docs/skill-packs.md`, `llms.txt`, `skills/heartbeat/SKILL.md`, `skills/memory-flush/SKILL.md` — all operator-customized, not touched by upstream this window; kept as-is.

### Operator config changed upstream (not auto-applied)
- **`aeon.yml`** — upstream added an inert `dev-loop` chain example (`workflow_dispatch`, chains `feature → pr-review`). Both skills are disabled here, so it's a no-op; copy the `chains:` block into your `aeon.yml` only if you want to run the provider-neutral coding loop.
- **`catalog/skills.json`, `catalog/packs.json`, `eyebrowlock.json`** — regenerated-not-copied by design. Not regenerated this run: the two skill merges changed only skill *bodies*, not frontmatter, so the catalogs and content-lock are unaffected (eyebrow-verify allows content drift for existing skills).

### Informational (upstream modified paths this fork doesn't track — kept absent)
`.claude/skills/aeon/references/secrets.md`; `skills/deploy-uni-hook/**` templates; `skills/remotion/project/package-lock.json`. Also: 8 `apps/*/package*.json` dependabot bumps are **already present locally** via this fork's own dependabot (#88 etc.) — skipped as already-synced.

### Upstream commits
| SHA | Summary |
|-----|---------|
| dc1a975 | llm-gateway(glm): pin reasoning effort to high (var-overridable) (#999) |
| 4f99198 | docs: remove MythosForge from ecosystem (#1010) |
| 69c6a17 | Bump eyebrow skill-integrity gate to v0.4.2 (#1002) |
| 66711d3 | feat(vuln-scanner): verify high severity claims (#1008) |
| f6d978b | chore(deps): bump tsx in /apps/cli (#1006) |
| 28e43dd | chore(deps-dev): bump @types/node (#1003) |
| 6dc0ad1 | chore(actions): bump actions/upload-artifact (#1011) |
| e7ef463 | Document optional runtime hardening for the Aeon MCP server (#1001) |
| 27a83d2 | chore(deps): hold eslint and globals majors so grouped PRs stay green (#1012) |
| 158a4ca | chore(deps): bump the dashboard group with 5 updates (#1014) |
| bccb67c | chore(deps-dev): bump wrangler in /apps/webhook (#1013) |
| c92fbfb | feat(chains): verify dev-loop pr handoff (#1009) |
| 6ae299f | chore(deps): bump qs in /apps/mcp-server (#1021) |
| de12993 | chore(deps): bump fast-uri in /apps/mcp-server (#1016) |
| c635427 | chore(deps): bump fast-uri (#1015) |
| 66ab2b4 | fix(chain-runner): stop a rejected dispatch from poisoning the reliability signal (#1017) |
| f3b3b29 | fix(skill-health): detect critical-incident recovery without waiting on lifetime success_rate (#1018) |
| bd74379 | fix(fleet-scorecard): stop non-skill runs from leaking into the per-skill reliability table (#1019) |
| 420dc85 | fix(notify): report per-channel delivery status and fail on total delivery failure (#1020) |
| d5f8717 | fix(deploy-uni-hook): scaffold comment points at _expectSwapRevert (#1022) |
| 339617b | fix(deploy-uni-hook): DeployHook.s.sol simulates cleanly for every hook kind (#1023) |
| bf33365 | fix(scheduler): read per-skill model: from block-style aeon.yml entries (#1024) |
