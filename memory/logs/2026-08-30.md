### Mention Radar
- **aeon:** QUIET (streak 4) - X.AI `x_search` 2026-08-23→08-30, HTTP 200, X_SOURCE=api: zero third-party mentions. Web sweep (Reddit/HN/PH/blogs/newsletters, incl. aiagentstore.ai's weekly AI-agent-news roundup): nothing fresh, no press this week.
- **miroshark:** 1 new mention (streak reset to 0) - X.AI `x_search` same window/method, HTTP 200, X_SOURCE=api: @webringonline asked a genuine product question in the operator's own thread ("does a sim reattach or lose its handle if the desktop tab closes mid-run?") - categorized Confusion, flagged as an engagement opportunity. Two other replies in the same thread (@svector_eth "hoping on it" 1 like, @danbuildss "This huge" 0 likes) were seen but omitted from the digest as non-signal (no content, near-zero engagement). Web sweep clean beyond already-known items.
- **GitHub deltas (vs 2026-08-28 baseline):** aeon 705★ (+9) / 252 forks (+7) - **crosses the +5-star notable threshold**, no single attributable driver found in-window; miroshark 1444★ (+2) / 299 forks (unch) - under threshold.
- **Top find:** @webringonline's sim-resilience question on MiroShark (tab-close mid-run behavior) - https://x.com/webringonline/status/2093733257473065031
- **Engagement opps:** 1 (@webringonline - answer directly in-thread)
- **Dropped:** none new this run; aeon remains past quiet_streak >= 2 (now streak 4) per contract rule 9 - digest-only, tracking continues.
- **Notification sent:** yes - aeon's +9-star GitHub jump and miroshark's new engagement opportunity both cleared the notify gate.
- **Note:** `${VAR}`-style brace expansion (`${NAME}`, `${FROM_DATE}`, etc.) and any shell output redirection (`>`) were both blocked in this run's sandbox, even for non-secret variables - worked around by using plain `$VAR` (no braces) for the rare local shell var, and writing the X.AI payload JSON files + final digest via the `Write` tool under `mrscratch/` (repo working dir, not `/tmp`) instead of `>` redirects, then feeding them to `secretcurl -d @file` / `-o file`. `mention-radar-seen.json`'s prior state (aeon streak 3, miroshark streak 4, mentions empty) was intact from 2026-08-27/08-28, so no reconstruction was needed this run. Scratch files under `mrscratch/` remain untracked/uncommitted (`commits: false`, no `rm` in the allowlist) - harmless.

### mention-radar — under the hood
- 🔧 41 tool calls — 25× Bash, 5× Write, 5× WebSearch, 5× Read, 1× ToolSearch  ·  actions: 1× gh, 2× git
