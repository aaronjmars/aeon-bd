# 2026-06-11

### setup — aeon + miroshark war room (Aaron + Nurstar)
- Installed Aaron's soul into `soul/` from `aaronjmars/soul-aaronjmars`: full `SOUL.md` (60K — identity, worldview, full opinions ledger w/ 2026 updates), `STYLE.md`, `examples/` (tweets, conversations, bad-outputs), `data/influences.md` + `data/substack/` essays. Skipped the 2.9MB raw Twitter JS exports.
- Tailored `STRATEGY.md` from unconfigured defaults → north-star = compounding adoption of aeon + miroshark; priorities = BD signal early, honest product state, timing-window ideas, depth on the two products, Aaron's voice. Audience = Aaron + Nurstar.
- Built 5 war-room skills: `product-pulse` (daily state), `bd-radar` (daily BD leads), `war-room` (daily standup, chain capstone), `sim-watch` (Tue weekly — Miroshark category intel), `idea-forge` (Fri weekly — idea engine).
- Wired `aeon.yml`: 5 skills registered `enabled: false`; daily `war-room` **chain** (product-pulse + bd-radar ∥ → war-room, 07:30 UTC, on_error: continue). YAML validated (203 skills).
- Updated `memory/watched-repos.md` (aeon + miroshark repo families + handles) and `MEMORY.md` (instance orientation).
- **Pending:** notification channel secret; dry-run to seed state; flip the chain on. No commit/PR yet (left in working tree for review).

### setup (round 2) — enable existing library skills + X mention monitoring
- Enabled 22 existing skills (`enabled: true` in `aeon.yml`): operator-scorecard, ecosystem-entrants, framework-watch, competitor-radar, idea-validator, launch-radar, x402-monitor, mcp-pulse, pm-pulse, pm-intel, compute-pulse, agent-displacement, agent-buzz, thread-writer, write-tweet, reply-maker, engagement-act, shiplog, show-hn, product-hunt, fetch-tweets, mention-radar. (23 enabled incl. heartbeat.) YAML revalidated.
- **X mentions:** `mention-radar` var set to `aeon, miroshark, @aeonframework, @miroshark_` (every-other-day web/social mention sweep) + `fetch-tweets` daily for bd-radar's X path.
- Wired idea loop: `idea-forge` now also appends kept ideas to `memory/topics/startup-ideas.md` (the shared backlog `idea-validator` + `launch-radar` read) — without this they'd SKIP.
- **Caveats surfaced:** (1) X depth needs `XAI_API_KEY` (or `scripts/prefetch-xai.sh`) — without it fetch-tweets/mention-radar fall back to WebSearch (lower quality, still works). (2) Monday morning is now stacked with weekly digests (mostly gated-notify). (3) war-room chain + sim-watch + idea-forge (my custom) still `enabled: false` — pending the operator's go.

### setup (round 3) — arm chain, even weekly spread, XAI secret
- Armed the centerpiece: `product-pulse`, `bd-radar`, `war-room`, `sim-watch`, `idea-forge` → `enabled: true` (war-room chain now live at 07:30 daily; 28 skills enabled total).
- **Even weekly spread** (was Monday-stacked) — 2 weekly skills/day Mon–Sat, 3 Sun, varied hours; idea pipeline kept in order: idea-forge (Wed 16:00) → idea-validator (Fri 18:00) → launch-radar (Sun 16:00). Daily X-content cluster staggered (fetch-tweets 17:00 → write-tweet 17:15 → reply-maker 17:20 → agent-buzz 17:30).
- **Secret:** set `XAI_API_KEY` on `aaronjmars/aeon-nur` via `gh secret set` (stdin). Repo already had `TELEGRAM_BOT_TOKEN`+`TELEGRAM_CHAT_ID` (notifications ready) + `CLAUDE_CODE_OAUTH_TOKEN` (gateway). Instance is config-complete — only `git push` remains to go live.
