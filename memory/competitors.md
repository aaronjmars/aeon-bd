# Competitor / reference pages to watch
#
# competitor-monitor reads this list every run (when its var is empty), snapshots
# each page's real signals (pricing, headings, CTAs, new/removed pages, title/meta),
# diffs against the previous run, and notifies only on a real change.
# One page per line as a markdown bullet. Add more with the skill's `add:<url>` var.

- https://www.nvidia.com/en-us/security/acknowledgements/
